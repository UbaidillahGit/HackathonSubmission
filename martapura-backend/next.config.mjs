/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['random.imagecdn.app',
      'picsum.photos',
      ],
  },
};

export default nextConfig;
